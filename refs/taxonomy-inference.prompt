# Taxonomy Inference Prompt · Phase 0 分类推断

> D-taxonomy：**LLM 主推断 + 规则后置兜底**。
> 判断力主体放 LLM（通用方法），规则只做隐私等安全约束的机械后置校验。

## 输入（Agent 拼装）

1. 目标仓库的目录树（`find . -type d -not -path '*/\.*' | head -50`，去隐藏目录）
2. 每个一级目录的样本文件名（每目录取 5 个 `.md` 文件名）
3. 抽样笔记内容前 200 字（共抽 5-10 篇）

## Prompt

```
你是一位知识管理架构师。我要给一个 Obsidian 个人知识库建立分类体系（TAXONOMY.md）。

## 目标仓库的现状

目录树：
{directory_tree}

各目录样本文件名：
{sample_filenames}

部分笔记内容摘录：
{sample_excerpts}

## 你的任务

推断一套适合这个仓库的**分类目录命名约定**。要求：

1. **基于实际内容推断，不强加任何预设分类**（不要套用"工作/学习/生活"这类通用模板，要从这个仓库真实的目录和内容里读出它的主题结构）
2. 提议一级分类目录的命名规则（如 `<NNN>【<主题>】` 前缀编号 + 全角括号主题，或纯主题名，或日期前缀——**沿用仓库现有命名习惯**，不要发明新约定）
3. 列出推断出的 5-15 个一级分类，每个给：名称、一句话定义、典型内容示例
4. 标注"收件箱"（根目录下未归类的散落笔记）与"归档区"（如有）的处理建议
5. 若现有目录结构混乱（如深层嵌套、命名不一致），诚实指出并给合并/简化建议——但**只建议、不替用户决定**

## 输出格式

```markdown
## 推断的分类体系

### 命名约定
<沿用现有 / 建议的命名规则，一句话>

### 一级分类
| 分类 | 定义 | 典型内容 |
|------|------|---------|
| ... | ... | ... |

### 收件箱与归档
- 收件箱：<根目录散落笔记的处置建议>
- 归档区：<是否检测到归档目录，命名是什么>

### 结构观察
<若发现命名不一致/深层嵌套/其他结构问题，诚实列出；否则写"无明显结构问题">
```
```

## 后置规则校验（Agent 在 LLM 输出后机械执行）

- 隐私目录检测：若 LLM 推断的分类中包含账号/密码/凭据相关目录（见 privacy-scan.patterns），强制标记为"隐私目录"并加入 category-privacy.md 保护清单，**不作为普通分类**
- 格式校验：输出是否含表格、是否给了命名约定；不合则要求 LLM 重写

## 输出去向

推断结果写入目标仓库 `docs/TAXONOMY.md`，**标记为 draft**（Phase 0 不替用户定分类；由 SKILL.md §Phase 0 决策表决策点 ② 交互定夺）。
