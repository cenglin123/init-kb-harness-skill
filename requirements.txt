# init-kb-harness — 脚本依赖（Phase 1-3 全量安装）
#
# 注：Python 标准库（os / sys / re / json / pathlib / hashlib / subprocess / socket /
#     posixpath / collections / dataclasses / typing / urllib / datetime / sqlite3 /
#     gzip / argparse / math / tempfile / shutil / csv / time / struct / inspect /
#     functools / concurrent.futures / __future__）不在本清单中。
#
# 第三方依赖：

requests>=2.28
python-frontmatter>=1.0
numpy>=1.24
networkx>=3.0

# office 文档提取（extract_office.py；缺装时对应格式跳过并提示）
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23
pypdf>=4.0
